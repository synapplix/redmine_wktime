<% other_formats_links do |f| %>
  <%= link_to 'CSV', url_for(:controller => controller.controller_name, :action => 'export', :startday => @startday, :user_id => params[:user_id]) + '&format=csv' %> | 
  <%= link_to 'PDF', url_for(:controller => controller.controller_name, :action => 'export', :startday => @startday, :user_id => params[:user_id]) + '&format=pdf' %>
<% end %>

<% html_title l(:label_spent_time), l(:label_details) %>

<% content_for :header_tags do %>
    <%= auto_discovery_link_tag(:atom, {:issue_id => @issue, :format => 'atom', :key => User.current.rss_key}, :title => l(:label_spent_time)) %>
<% end %>

<!-- Link to the /time_entries -->
<% extendedPath = '?utf8=✓&f[]=user_id&op[user_id]=%3D&v[user_id][]=me&f[]=spent_on&op[spent_on]=l2w&f[]=project_id&op[project_id]=%3D&v[project_id][]=mine&f[]=&c[]=spent_on&c[]=user&c[]=project&c[]=issue&c[]=hours&c[]=comments' %>
<%= link_to(l(:label_wk_evaluation), time_entries_path + extendedPath)  if User.current.allowed_to?(:view_time_entries, nil, :global => true) %>